version: '3.8'

services:
  simulator:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/home/node/app/src
    networks:
      - mongodb_network
    container_name: simulator
    expose:
      - ${PORT}
    ports:
      - ${PORT}:${PORT}
    command: npm run start